import { WaterCycleBuilderSystem } from './water-cycle-builder-system';

describe('WaterCycleBuilderSystem', () => {
  it('should create an instance', () => {
    expect(new WaterCycleBuilderSystem()).toBeTruthy();
  });
});
