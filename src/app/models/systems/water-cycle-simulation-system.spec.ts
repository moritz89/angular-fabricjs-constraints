import { WaterCycleSimulationSystem } from './water-cycle-simulation-system';

describe('WaterCycleSimulationSystem', () => {
  it('should create an instance', () => {
    expect(new WaterCycleSimulationSystem()).toBeTruthy();
  });
});
