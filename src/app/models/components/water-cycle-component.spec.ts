import { WaterCycleComponent } from './water-cycle-component';

describe('WaterCycleComponent', () => {
  it('should create an instance', () => {
    expect(new WaterCycleComponent()).toBeTruthy();
  });
});
